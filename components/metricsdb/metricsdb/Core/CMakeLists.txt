add_library(mdb_core
  STATIC
    Database.cpp
    Database.hpp
    CommonTypes.hpp
)
target_include_directories(mdb_core
  PUBLIC
    "${PROJECT_SOURCE_DIR}"
)
target_link_libraries(mdb_core
  mdb_proto
)